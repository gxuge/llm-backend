version: '3.8'
services:
  api:
    build: .
    container_name: fastapi-langgraph
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
    networks:
      - share-net

networks:
  share-net:
    external: true