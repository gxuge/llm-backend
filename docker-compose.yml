# ??????????????????
services:
  api:
    image: ${IMAGE_NAME}:${IMAGE_TAG}
    container_name: fastapi-langgraph
    restart: always
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
